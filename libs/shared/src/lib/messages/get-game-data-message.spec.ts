import { GetGameDataMessage } from './get-game-data-message';

describe('GetGameDataMessage', () => {
  it('should create an instance', () => {
    expect(new GetGameDataMessage()).toBeTruthy();
  });
});
